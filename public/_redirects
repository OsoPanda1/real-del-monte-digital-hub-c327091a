# SPA fallback para React Router — Cloudflare Pages lo maneja automaticamente
# al detectar react-router como framework. No necesita regla /* /index.html.
# Redireccion apex→www se configura en Cloudflare Dashboard > Page Rules.
